@model List<PetGrooming.Models.Species>


@{
    ViewBag.Title = "List";
}

<h2>List of Species</h2>
@{ 
    foreach (var species in @Model) {

        <div>
            @species.Name
        </div>
    }
}